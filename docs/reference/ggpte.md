# ggpte

Deprecated. Use `autoplot()` on the `pte_results` object instead.

## Usage

``` r
ggpte(pte_results)
```

## Arguments

- pte_results:

  a `pte_results` object

## Value

a `ggplot` object
