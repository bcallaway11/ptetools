# Sanity Checks on Critical Values

A function to perform sanity checks and possibly adjust a a critical
value to form a uniform confidence band

## Usage

``` r
crit_val_checks(crit_val, alp = 0.05)
```

## Arguments

- crit_val:

  the critical value

- alp:

  the significance level

## Value

a (possibly adjusted) critical value
