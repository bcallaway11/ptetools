# summary.pte_results

A function to summarize `ptetools` results.

## Usage

``` r
# S3 method for class 'pte_results'
summary(object, ...)
```

## Arguments

- object:

  an `pte_results` object

- ...:

  other arguments

## Value

a `summary.pte_results` object
